import MenuWrapper from "../menu/MenuWrapper";

const Mentorias = () => {
    return (
        <div>
            <MenuWrapper whichIsActive='mentorias' />
            <h1>Mentorias</h1>
        </div>
    );
}

export default Mentorias;