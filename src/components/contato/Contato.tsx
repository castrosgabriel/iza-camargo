import MenuWrapper from "../menu/MenuWrapper";

const Contato = () => {
    return (
        <div>
            <MenuWrapper whichIsActive='contato' />
            <h1>Contato</h1>
        </div>
    );
}

export default Contato;